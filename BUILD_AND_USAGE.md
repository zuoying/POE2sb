# POE2sb Gamepad Synchronizer - 构建和使用指南

## 项目概述

POE2sb是一个基于Waveshare RP2350-USB-A板的固件项目，用于将单个GameSir T4 Kaleid有线手柄转换为双虚拟XInput控制器输出到PC。主要特性包括：

- 读取GameSir T4 Kaleid（VID:0x3537, PID:0x100E）HID手柄
- 输出两个虚拟XInput控制器到PC
- 三种工作模式：SYNC（同步）、MAIN（仅主）、SUB（仅副）
- 反作弊随机偏移（仅虚拟控制器2）
- WS2812 LED状态指示和自动亮度调节

## 硬件要求

1. **主控板**：Waveshare RP2350-USB-A
2. **输入设备**：GameSir T4 Kaleid有线游戏手柄（或其他兼容HID手柄）
3. **连接**：
   - USB-C端口（设备模式）连接到PC
   - USB-A端口（主机模式）连接GameSir手柄
4. **LED**：WS2812 RGB LED连接到GPIO16

## 软件构建

### 环境准备

```bash
# 安装必要的工具链
# 根据你的操作系统安装Pico SDK和CMake
# 参考：https://datasheets.raspberrypi.com/pico/getting-started-with-pico.pdf
```

### 构建步骤

```bash
# 创建构建目录
mkdir build
cd build

# 配置项目
cmake ..

# 编译固件
make -j4

# 编译完成后，固件将生成在build/目录下
# 主要输出文件：poe2gamepad.uf2
```

### 快速构建（使用脚本）

```bash
# 可以使用提供的构建脚本（如果需要）
# 确保设置了PICO_SDK_PATH环境变量
```

## 烧录固件

1. 按住RP2350板上的BOOTSEL按钮
2. 通过USB-C线连接电脑
3. 释放BOOTSEL按钮，板子将进入USB大容量存储模式
4. 将`poe2gamepad.uf2`文件拖放到出现的RPI-RP2驱动器
5. 等待自动重启

## 使用方法

### 基本连接

1. **连接GameSir手柄**：将手柄插入RP2350板的USB-A端口
2. **连接PC**：将RP2350板的USB-C端口连接到电脑
3. **启动**：设备将自动初始化并识别手柄

### LED状态指示

| LED颜色 | 状态说明 | 亮度 |
|---------|----------|------|
| 蓝色 | 系统初始化中 | 低 |
| 青色 | 系统就绪，等待手柄 | 低 |
| 绿色 | 手柄已连接 | 高 |
| 绿色 | SYNC模式 | 高 |
| 蓝色 | MAIN模式（仅主控制器） | 高 |
| 红色 | SUB模式（仅副控制器） | 高 |
| 红色 | 错误状态 | 高 |

### 工作模式切换

**切换方法**：同时按下手柄的**Menu(Start) + View(Back)**按钮并保持1秒

**三种工作模式**：
1. **SYNC模式**（绿灯）：两个虚拟控制器都输出，副控制器应用反作弊偏移
2. **MAIN模式**（蓝灯）：只输出虚拟控制器1（主控制器）
3. **SUB模式**（红灯）：只输出虚拟控制器2（副控制器），应用反作弊偏移

### 反作弊随机偏移

仅在**SYNC模式**和**SUB模式**中对虚拟控制器2生效：

- **摇杆偏移**：±4单位
- **扳机偏移**：±2单位  
- **延迟偏移**：1-6毫秒

偏移值在初始化时随机生成，可定期更新以增加随机性。

## 技术特性

### HID到XInput转换

固件自动检测手柄报告格式并转换：
- **XInput格式**（19-20字节）：直接映射
- **DInput格式**（8字节）：格式转换和按钮映射
- **标准HID格式**（6-10字节）：通用转换

### 双核心架构

- **Core0**（主核心）：USB设备模式、LED管理、模式切换
- **Core1**（辅助核心）：USB主机模式、HID手柄读取

### USB配置

- **设备模式**：双HID接口（CFG_TUD_HID=2）
- **主机模式**：HID设备支持（CFG_TUH_HID=1）
- **PIO-USB**：同时支持USB主机和设备功能

## 故障排除

### 常见问题

1. **手柄未被识别**
   - 检查VID/PID是否正确（0x3537/0x100E）
   - 确保手柄直接连接到RP2350的USB-A端口
   - 检查电源是否开启（LED应为青色/绿色）

2. **PC未检测到虚拟控制器**
   - 检查USB-C连接
   - 确保设备枚举成功（查看设备管理器）
   - 检查固件是否正确烧录

3. **LED不亮或不正确**
   - 检查GPIO16连接
   - 确认LED电源和接地
   - 检查PIO程序是否正确加载

4. **模式切换不工作**
   - 确认Menu和View按钮功能正常
   - 确保同时按下并保持1秒
   - 检查串口输出确认模式切换事件

### 调试信息

固件通过串口输出调试信息（115200波特率）：
- 系统初始化状态
- 手柄连接/断开事件
- 模式切换事件
- 定期系统状态报告
- HID报告格式检测信息

## 扩展开发

### 添加新手柄支持

1. 在`class/hid/hid_host.c`中修改`_is_gamepad_device()`函数
2. 添加新VID/PID到允许列表
3. 如果需要，更新`hid_to_xinput_convert()`中的转换逻辑

### 修改工作模式

1. 在`mode_manager.c`中调整模式切换逻辑
2. 修改按钮组合或长按时间
3. 调整反作弊偏移参数

### 自定义LED行为

1. 在`led_manager.c`中修改颜色定义
2. 调整亮度级别
3. 添加新的LED状态

## 许可证和安全

本项目仅供学习和研究用途。使用本固件时请遵守：
- 游戏服务条款和最终用户许可协议
- 反作弊系统政策
- 当地法律法规

## 支持与贡献

如有问题或建议，请：
1. 查看串口调试输出
2. 检查硬件连接
3. 参考技术文档
4. 在项目仓库中提交问题

---

**注意**：正确使用游戏手柄同步设备，遵守游戏规则和道德准则。